const chalk = require('chalk');

// log(chalk.green.bold('Start build...'));

// const log = (message) => {
//   return console.log(chalk.bgWhite.black.bold(' pbstyles ') + ' Build: ' + message);
// };
